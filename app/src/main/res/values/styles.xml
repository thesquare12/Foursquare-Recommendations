<resources>

    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!--<item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>-->
    </style>

    <!--<style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>-->

    <style name="Detail" />

    <style name="Detail.CardView">
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="cardCornerRadius">@dimen/list_item_card_corner_radius</item>
        <item name="cardElevation">4dp</item>
    </style>

    <style name="Detail.Title">
        <item name="android:textColor">@color/detail_item_title</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="Detail.Content">
        <item name="android:textColor">@color/detail_item_content</item>
        <item name="android:textSize">14sp</item>
    </style>
</resources>
